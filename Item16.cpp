// Item 16: Use the same form in corresponding uses of new and delete.
/*
  Abstain from typedefs for array types to avoid confusion because it
  becomes hard to know that delete[] had to be called to destroy
 */

